$("table#collaborators-table tbody").append("<%= escape_javascript(render 'collaborator', :user => @collaborator, :bot => @bot) %>")

$("select.add-collaborator-picker option[value='<%= @collaborator.id %>']").remove()
$(".add-collaborator-picker").selectpicker('refresh')
